# knexbridge CLI

Command line interface for generating TypeScript types and Zod schemas from a Knex-managed database schema.

## Installation
`ash
npm install -g knexbridge
`

## Usage
`ash
knexbridge generate --config ./knexfile.js --out ./generated
`

See the KnexBridge repository for full documentation.
