{
  "name": "knexbridge-monorepo",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/core",
      "packages/cli"
    ]
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present"
  }
}
