{
  "name": "knexbridge-monorepo",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspace knexbridge-core && npm run build --workspace knexbridge",
    "build:core": "npm run build --workspace knexbridge-core",
    "test": "npm run build:core && node --test"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
